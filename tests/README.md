# Tests

Add small checks as the project grows. High-priority checks include:

- no feature timestamp is later than the prediction issue time `T`;
- all selected rows match the intended Route A direction and target stop;
- delay sign convention is consistent;
- train/validation/test date ranges never overlap.

